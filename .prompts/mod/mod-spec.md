# Moderate Specification

Analyze `spec.md` to be sure it aligns with the details captured in the aggregation of checkpoints located in `.checkpoints`. Make any necessary corrections and optimizations, then output thea adjusted document into an artifact. Artifact only. No preamble or summary.